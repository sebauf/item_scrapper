# Dépendances de test uniquement — jamais installées dans l'image Docker.
-r requirements.txt
pytest==8.3.4
# mongomock exécute réellement les pipelines d'agrégation : les tests de refine
# vérifient le $group/$push tel que Mongo l'interprète, pas un bouchon maison.
mongomock==4.3.0
