apiVersion: v1
kind: Service
metadata:
  name: price-tracker-mcp
  namespace: price-tracker
spec:
  selector:
    app: price-tracker-mcp
  ports:
    - port: 80
      targetPort: 3010
  type: ClusterIP
